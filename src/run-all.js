'use strict';

/**
 * Asserts "expected" versus "actual",
 * 'failing' the assertion (via Error) if a difference is found.
 *
 * @param {String} label The test suite label ie. "validation rules for credit cards"
 * @param {Array} tests Definition of test cases to assert
 *
 * @return {Array} results of the tests asserted
 */
module.exports = function runAll(label, tests) {

  return [];
};
